{{template "head" .IsSignedIn}}
<div id="attendees">
    <h1>Attendees</h1>
    <ul>
        {{range $name := .Attendees}}
        <li>{{$name}}</li>
        {{end}}
    </ul>
</div>
{{template "foot"}}